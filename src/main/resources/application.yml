spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:oracle:thin:@//localhost:51521/XE
    username: system
    password: dev1990
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true # Ajuda se estiver tentando acessar de fora
  security:
    user:
      name: todo
      password: "123456"
      roles: ADMIN, USER, MANAGER